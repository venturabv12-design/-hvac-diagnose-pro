{
  "name": "mike-watchdog",
  "version": "1.0.0",
  "private": true,
  "description": "Always-on watchdog for Mike (trazermike.io). Health-checks every minute; when Mike is down it calls Brandon via Cello. Read-only — never modifies Mike.",
  "main": "index.js",
  "scripts": { "start": "node index.js" },
  "engines": { "node": ">=18" }
}
